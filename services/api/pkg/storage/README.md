# pkg/storage

S3-compatible object storage (document 12). MinIO runs locally from Phase 1 so
the infrastructure is reproducible, but no application code uploads anything
until documents and proof-of-delivery arrive in their own slices.
